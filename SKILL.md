---
name: i-am-caption
description: Transform a user-supplied photo by keeping the photograph completely truthful and overlaying one first-person "I AM …" statement in the classic Nikon "I AM NIKON" yellow-caption style — a solid yellow block with bold black "I AM" joined to a yellow-outlined block with white uppercase statement, centered on the frame. The statement is distilled from the image and reveals the story beyond the frame. Use when the user mentions I AM 字幕、黄字宣言、尼康广告风、给照片加一句宣言/字幕、photo caption statement, or wants a photo turned into an I-AM-style statement poster.
---

# I AM Caption · 黄字宣言字幕

把一张用户照片变成尼康经典「I AM NIKON」广告式的宣言作品：**照片原样保留，一句话替画面里的人（或物、或在场者）说出画面之外的故事。**

核心签名：**真图为底、宣言为魂、黄字成链、一句千言**。

- 照片不重绘、不调色、不加滤镜，只叠加一层字幕；
- 文案是灵魂：第一人称 "I AM …"，基于画面可见事实，指向画面之外的故事；
- 样式必须精确复刻双块结构：左侧黄底黑字 "I AM"，右侧黄框白字宣言；
- 一句话要让人在看到照片第二眼时，读出照片没拍到的部分。

默认返回成品图 + 一句创作说明。只在用户明确要求时给出完整提示词或排版参数。

## Workflow

1. **读图**：观察照片的主体、动作、光线、时间、空间关系与情绪张力。
2. **建 Story Card**（内部完成，不输出）：画面里正在发生什么？定格前后一秒发生了什么？这个人/物是谁？他不会对镜头说什么？
3. **提炼文案**：用文案引擎（下节）得出一句 "I AM …" 宣言。
4. **排版渲染**：调用 `scripts/render_i_am_caption.py` 以 PIL 精确排版（文字零拼错、颜色精确），先按几何中心渲染并回看成图；若主体被遮挡，仅改 `--y` 上下微调重渲（水平不动），直到主体完整可见。仅当用户要求文字与画面光影融合的质感时，改用 AI 生成模式并读取 `references/i-am-caption-prompt.zh-CN.md`。
5. **自检**：对照 Quality Gate 核验后返回。

## Copywriting Engine（文案引擎）

### 视角选择

按画面内部判断代入者：

- 画面有明确主体（人、动物、有性格的物件）→ 代入**主体本人**的第一人称；
- 画面无主体（空景、静物、城市、天象）→ 代入**在场者（摄影师）**或**场景拟人**；
- 多主体 → 选最有故事张力的那一个，不贪多。

### 五个内容方向

从画面事实出发，选最有张力的一个方向：

1. **身份宣言**：画面所见与真实身份的反差。照片拍的是一个孩子骑车 → "I AM THE FUN CAVALRY"。
2. **瞬间放大**：把定格的一瞬说成一种存在状态。宇航员漂向飞船 → "I AM HOME"。
3. **画外故事**：照片前后一秒正在发生、但没被拍到的事。凌空跳跃 → "I AM ALMOST FLYING"。
4. **反差幽默**：画面平凡琐碎，宣言宏大庄重。猫端坐沙发 → "I AM IN CHARGE HERE"。
5. **关系宣言**：与画面另一主体的关系。两人背影 → "I AM STILL HERE WITH YOU"。

### 五种句式抽屉

官方原片出现过五种语法模式（完整清单见 [docs/caption-patterns.md](docs/caption-patterns.md)）。先判断画面量级与错位方向，再从对应抽屉造句：

| 抽屉 | 句式 | 官方示例 | 适用 |
| :--- | :--- | :--- | :--- |
| 冠名式 | I AM THE + 名词短语 | THE NIGHT · THE BEAUTY OF LIFE | 氛围图、群像、有明确气质的街拍（主力句式） |
| 传奇式 | I AM + 专有名词 | MARCO POLO · BUFFALO BILL | 旅行、探险、夸张姿势 |
| 身份式 | I AM + A/AN + 名词 | A WHITE SHARK · A HOPELESS ROMANTIC | 动物、特写、情绪场景 |
| 状态式 | I AM + 状态词/短语 | HOME · NOTHING · WHATEVER IT TAKES | 极简构图、大留白 |
| 归属式 | I AM PART OF … | PART OF THE WORLD | 群像、系列合集 |

按照片类型选抽屉的优先级：

| 照片类型 | 优先抽屉 | 词库示例 |
| :--- | :--- | :--- |
| 街拍/人物剪影 | 冠名式、状态式 | THE MORNING RUSH · NOT IN A HURRY · EVERYONE'S EXTRA |
| 建筑/空间 | 冠名式 | THE CITY'S PAUSE · STILL UNDER CONSTRUCTION |
| 动物/宠物 | 身份式、传奇式 | THE LANDLORD · A LEGEND · BOSS AROUND HERE |
| 静物/食物 | 身份式反差 | THE MAIN COURSE · WORTH THE WAIT · NOT FOR SHARING |
| 风光/天空 | 冠名式、状态式 | THE BIG PICTURE · BRIEFLY MAGNIFICENT · ON FIRE |

**副词是幽默的开关**：ALMOST / NOT / STILL / BRIEFLY 这类词制造「差一点」与「偏不」，优先考虑。

### 三道检验

句子定稿前必须通过，不过就重写：

1. **画外测试**：句子的信息量至少一半在画面之外——看到图猜不到的才算数。
2. **唯一性测试**：把句子盖到另一张照片上若仍然成立，即不合格。"I AM BRIEFLY MAGNIFICENT" 换到火山之外就塌，合格；"I AM YOUR NINE TO FIVE" 换到任何写字楼图都成立，偏弱。
3. **错位测试**：宣言的量级要与画面错开至少一档（画面小宣言大，或画面大宣言小）。同量级直接毙（画面美 → "I AM BEAUTIFUL" ✗）。

### 硬性规则

- 格式：以 "I AM" 开头，全大写英文，含 "I AM" 共 **2–6 个单词**，无标点、无句号、无感叹号。
- **信**：必须能对应画面中真实存在的事实（主体、动作、光线、空间、时间）。
- **达**：口语、直白、一眼读懂，不用生僻词和修辞堆砌。
- **雅**：留画外余韵——读者要能脑补出照片没拍到的那部分故事。
- 禁止：直接复述画面（"I AM A BOY ON A BIKE" ✗）、品牌广告腔（"I AM NIKON" ✗）、空泛大词单独成句（"I AM MOMENT / DREAM / LIFE" ✗）、与画面无关的抒情。
- 默认英文。仅当用户明确要求时用中文「我是 …」（≤10 字），排版规则同英文。
- 只产出一句。不输出候选列表，先内部定稿再渲染。

## Caption Style Spec（样式规格）

复刻 Nikon「I AM」广告的黄色字幕条，压在照片上：

- **颜色**：尼康黄 `#FFE000`；"I AM" 用近黑 `#111111`；右侧宣言用纯白（可 92% 不透明）。
- **结构**：两个等高矩形横向并列、**无缝相接**——左块的右缘就是右块的左缘，两块顶边对齐、底边对齐：
  - 左块：黄色实心矩形，内含黑色特粗无衬线 **"I AM"**；
  - 右块：透明底 + 黄色细描边矩形（描边宽约为块高的 3.5%，描边向内画），内含白色常规字重大写宣言，左右留白约为块高的 28%。
- **比例**：文案越长整条自动缩小，总宽不超过画面宽度的 **86%**，绝不换行；块高按方向分档——竖幅约为短边的 **12%**，横幅约为短边的 **10.5%**（横幅宣言比竖幅小一档）；短文案可用 `--fill 0.75` 按目标宽度比例放大（长文案会被 86% 上限重新压回）。
- **位置**：**几何中心优先**——水平永远居中，默认垂直 50%。中心与主体相交时的避让顺序：**① 先向下找**最近的不相交位置 → **② 下方没有净空再向上找**。候选位置中**优先大面积纯色/均匀色块**（纯白、纯黑、纯蓝天、纯绿地、虚化背景等），碎纹理区域让位于均匀区域。渲染后**以全画面回看遮挡**（不能只看字幕条局部裁片，条两端压到主体边缘最容易漏判）：条不得与主体任何部分相交；水平永远保持居中；调整幅度尽量小（优先选最近的干净区），永不贴边。若全画面确实无净空横带（如满构图俯拍），允许用 `--scale 0.85` 微缩字幕条再避让，宁可条压过次要陪体，也不压主体。**前景杆线（晾衣杆、电线、栏杆）不算主体，视为背景可以压**；**画面底部的黑色/暗部区域是优质候选位**——白字天然高对比，无需加衬底。
- **字体气质**：接近 Helvetica/Arial 的中性无衬线——左块特粗（Bold），右块常规（Regular），右块字距略放宽。
- 照片其余像素一律不动：无暗角、无光晕、无投影、无压暗遮罩。
- **浅色背景判断衬底**：渲染后回看，若字幕条所在位置背景偏浅（白墙、亮天空、浅水面、雪地等）导致白字/黄框对比不足，则加 `--backing` 给右块叠加半透明黑色衬底（默认 18% 不透明度，可用 `--backing 0.35` 调整）；背景本身够深或对比足够则不加，保持画面干净。

## Rendering

默认模式（精确排版）：

```bash
python3 scripts/render_i_am_caption.py <input_photo> \
  -t "THE FUN CAVALRY" \
  -o <output_path>
```

常用参数：`--fill 0.75`（按目标宽度比例缩放字幕条）、`--y 0.42`（覆盖垂直位置，仅用户要求时用）、`--scale 0.9`（整体缩放）、`--zh`（中文模式，文案写「我是 …」内容）、`--backing`（浅色背景时右块加半透明黑衬底，默认 18%，可传 `--backing 0.35` 调整不透明度）。

**后编辑（默认开启）**：渲染时加 `--html <path>.html`，同时产出一个独立 HTML 编辑器——在浏览器里可直接改文字（点击图上色块或顶部输入框）、拖滑杆调大小/位置/黄色色值、开关衬底，并一键导出与预览像素一致的全尺寸 PNG。几何算法与脚本完全同源，导出结果与 PIL 渲染一致。

AI 生成模式（需要文字融进画面光影时才用）：读取 `references/i-am-caption-prompt.zh-CN.md` 编译提示词，随原图送图像生成服务；该模式必须用 Quality Gate 逐字校验渲染后的拼写。

## Guardrails

- **方向保真**：先按 EXIF orientation 摆正照片再叠加字幕（脚本已内置 `exif_transpose`），竖幅照片输出竖幅、横幅照片输出横幅，与用户在系统相册里看到的方向一致。
- 用户照片是唯一内容来源，除字幕条外不添加任何元素、不修改任何像素。
- 不添加 Nikon logo、品牌角标、日期、签名、水印，除非用户明确要求。
- 字幕条永远横排、单行、双块结构，不得改成竖排、三块、圆角或渐变。
- 文案与照片必须同源：换一张照片就要重写一句，不套模板。
- 若画面主体面部在中心，仅做上下（垂直）调整避让，水平保持居中；优先选离中心最近的干净位置，不缩小字幕、不水平偏移。

## Quality Gate

返回前核验：

- 照片方向是否与原片观看方向一致（竖进竖出、横进横出）？
- 照片是否原样保留（无调色、无裁切、无 AI 痕迹）？
- 文案是否第一人称、2–6 词、全大写、无标点？
- 文案是否通过三道检验（画外、唯一性、错位）？
- 文案是否基于画面事实、且指向画面之外的故事（而不是复述画面）？
- 双块结构、尼康黄 `#FFE000`、黑 "I AM"、白字黄框是否精确？两块是否无缝相接、顶底对齐？
- 整条宽度是否 ≤86% 画面宽、单行不换行、拼写逐字正确？
- 位置是否几何中心优先？若中心遮挡主体，是否只做了垂直调整且水平仍居中、未贴边？
- 字幕条处背景是否偏浅？白字对比是否足够，不足时是否已加 `--backing` 衬底？
- 是否没有添加任何 logo、日期、水印或多余元素？

## Output Format

```markdown
![I AM Caption](absolute-image-path)

**这句宣言**

[一句话：说明代入的视角、画面事实、以及宣言指向的画外故事。]
```
